
public class MenuEnums {
	public enum MAIN_MENU { ZERO, GAME, OPTIONS, HELP; }
    public enum GAME { ZERO, REGISTER, NEW, QUIT; }
    public enum OPTIONS { ZERO, HISTORY, HIGH_SCORE, DIFFICULTY; }
    public enum HELP { ZERO, ABOUT; }
    public enum DIFFICULTY { ZERO, NOVICE, INTERMEDIATE, ADVENCED; }
}
